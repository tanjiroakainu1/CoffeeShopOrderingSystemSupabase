/** Public APK served from `react-web/public/downloads/` (copied by `scripts/copy-apk.mjs`). */
export const APP_DOWNLOAD = {
  apkPath: "/downloads/roast-bean.apk",
  fileName: "roast-bean.apk",
  label: "Roast & Bean for Android",
  version: "1.0.0",
} as const;
